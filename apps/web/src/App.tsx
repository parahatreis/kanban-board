import { SHARED_VERSION } from "shared/meta";

export default function App() {
  return (
    <main>
      <h1>Kanban board</h1>
      <p>Workspace shared package: {SHARED_VERSION}</p>
    </main>
  );
}
