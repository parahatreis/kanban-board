# Drizzle migrations

SQL migrations generated by Drizzle Kit are written here after you run:

```bash
yarn workspace api db:generate
```

Apply them with:

```bash
yarn workspace api db:migrate
```

Ensure `DATABASE_URL` is set (see repo root `.env.example`). This folder may be empty until the first `db:generate`.
